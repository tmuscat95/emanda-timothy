{
  "name": "tasks",
  "private": true,
  "workspaces": [
    "/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "prettier": "~3.6.2",
    "typescript": "^5.0.0"
  }
}
